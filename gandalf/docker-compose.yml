version: "3"

services:
   web:
      build:
         context: .
         dockerfile: dev.Dockerfile
      container_name: new-question
      volumes:
         - ./src:/var/www/html
      ports:
         - "8087:80"
